
#pragma CIVL ACSL

int y = 0;

// this contract is a lie, but CIVL respects it when doing POR...
/*@ pure; */
$atomic_f int f(int x) {
  y = x;
}

int main(void) {
  $parfor (int i:0 .. 1) f(i);
  $print(y);
  $assert(y==1);  // OK since f is believed to be pure
}
